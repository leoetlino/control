stages:
  - test
  - deploy

test:
  stage: test
  script:
    - npm install
    - npm test
  except:
    - master

deploy:
  stage: deploy
  script:
    - npm install
    - npm test
    - npm run build
    - ./deploy.sh
  only:
    - master
